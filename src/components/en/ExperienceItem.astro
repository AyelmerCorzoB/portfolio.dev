---
import LinkInline from "./LinkInline.astro"

interface Props {
  title: string
  company: string
  description: string
  link?: string
  date: string
}

const { title, company, description, link, date } = Astro.props
---

<div class="group relative pl-8 pr-2 py-6 transition-all duration-300 hover:bg-gray-50 dark:hover:bg-gray-800/30 rounded-xl">
  <!-- Timeline connector -->
  <div class="absolute left-0 top-0 bottom-0 w-0.5 bg-gradient-to-b from-yellow-400 via-yellow-300 to-transparent"></div>
  
  <!-- Timeline dot -->
  <div class="absolute left-[-8px] top-7 h-4 w-4 rounded-full bg-yellow-400 shadow-[0_0_10px_rgba(250,204,21,0.5)] transition-all duration-300 group-hover:scale-110"></div>
  
  <div class="flex flex-col md:flex-row gap-4">
    <!-- Left column (date and company) -->
    <div class="md:w-1/3 space-y-1">
      <time class="text-sm font-medium text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-800 px-2 py-1 rounded-md inline-block">
        {date}
      </time>
      <h4 class="font-semibold text-lg text-gray-700 dark:text-white transition-colors duration-300 group-hover:text-yellow-600 dark:group-hover:text-yellow-400">
        {company}
      </h4>
    </div>
    
    <!-- Right column (title and description) -->
    <div class="md:w-2/3 space-y-3">
      <h3 class="text-xl font-bold text-yellow-500 dark:text-yellow-400">
        {title}
      </h3>
      
      <div class="text-gray-600 dark:text-gray-300 leading-relaxed">
        {description}
      </div>
      
      {
        link && (
          <LinkInline href={link} class="inline-flex items-center gap-1 text-yellow-600 dark:text-yellow-400 font-medium hover:underline mt-2 group-hover:translate-x-1 transition-transform">
            Saber más
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 transition-transform group-hover:translate-x-1"
              viewBox="0 0 24 24"
              stroke-width="2.5"
              stroke="currentColor"
              fill="none"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M9 6l6 6l-6 6" />
            </svg>
          </LinkInline>
        )
      }
    </div>
  </div>
</div>

